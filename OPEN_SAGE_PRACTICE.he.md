# משחק תרגול מול Open Sage

## מקור המנוע

כל הערכות המהלכים מגיעות מ־[markbgsage/bgsage](https://github.com/markbgsage/bgsage),
בגרסה שכבר נעולה ב־`backgammon-analysis-service/requirements.txt`:
`d8325a491168062df1047ffd998f3a5dfb426a0c` (`2.0.20260907`).
המקור אומת גם דרך `direct_url.json` של החבילה המותקנת.

קובצי המקור שעליהם מבוסס החיבור: `python/bgsage/analyzer.py` לממשק
`BgBotAnalyzer.checker_play`, `board.py` לייצוג לוח בן 26 משבצות ולמהלכים
חוקיים, `types.py` למבנה התוצאות. לא הועתק קוד מנוע לתוך הפרויקט.

## מה מומש

- `/tournaments/practice`: בחירת רמה ופתיחת משחק לנקודה אחת ללא שעון או הכפלות.
- כרטיס כניסה חתום, קצר־חיים וחד־פעמי, עם salt נפרד מכרטיסי טורניר.
- משתמש האתר מקושר לזהות קיימת בשרת המשחק; שחקן לבן אנושי מול שחור Open Sage.
- משחק דרך WebSocket ותשתית חוקי המשחק הקיימת בשרת. הקוביות מגיעות משירות הקוביות הקיים.
- המנוע בוחר **לוח סיום לתור שלם**. השרת משחזר רצף חוקי דרך `make_move`
  ומאמת אותו לפני הביצוע. אין קבלת לוח משחק מהדפדפן.
- נעילה זמנית במסד הנתונים מונעת משני חיבורים או תהליכים להפעיל את אותו הבוט.
  לוח היעד נשמר לפני ביצוע התור כדי לאפשר המשך גם אחרי הפעלה מחדש באמצע התור.
- תוצאה מסוג `ai` נשמרת עם המשתמש האנושי. אין יצירת טורניר, חיוב ארנק או דיווח דירוג.

## רמות

כל הרמות משתמשות בהערכת `1ply` של Open Sage, עם `away1=away2=1` ו־Crawford.
קשה בוחרת את המועמד הראשון. בינוני וקל דוגמות מועמדים על פי equity, עם חלון
טעות מרבי של 0.08 / 0.30 וטמפרטורה 0.025 / 0.10 בהתאמה.
אלה **הגדרות מוצר שלנו**, לא דרגות רשמיות של Open Sage ולא דירוגי שחקן מכוילים.

## הפעלה

1. בשרת המשחק להריץ `python manage.py migrate` (מיגרציה חדשה `0017_aisession`).
2. להגדיר בשרת המשחק `AI_SERVICE_URL` לכתובת שירות הניתוח/עובד הבוט,
   לדוגמה `http://127.0.0.1:8006`, ו־`ANALYSIS_API_TOKEN` לערך זהה לזה שבשירות הניתוח.
   המפתח פרטי; אין להעבירו ב־VITE או לדפדפן.
3. להפעיל מחדש את שירות הניתוח, שרת המשחק ושרת האתר ולבנות את שני הממשקים.
   חיבור GAMELINK הרגיל (כתובות, issuer, audience ומפתחות כרטיסים) צריך להיות מוגדר.
4. בתצורת עומס מומלץ להפעיל עובד נפרד למשחק חי מתוך אותו שירות,
   כדי שניתוח משחקים ארוך לא יתחרה בתור הבוט. בשימוש מקומי אפשר להשתמש בשירות הקיים.

ה־API הפנימי החדש: `POST /api/v1/internal/bot/move/`, עם Bearer token,
גוף `{state, difficulty}` ותוצאה `{board, engine, engine_version, eval_level}`.
הלקוח מגביל המתנה ל־8 שניות. עובד עסוק/תקלה מחזיר 503, המשחק נשאר במצבו
האחרון ואפשר לחזור אליו דרך מסך התרגול. אין מעבר שקט לבוט הישן.

## גבולות הגרסה הראשונה

- אין בוטים בתוך שיבוצי טורניר; הכניסה לתרגול היא מאתר הטורנירים.
- אין עדיין סדרות, קוביית הכפלה או מסך ניתוח למשחקי AI. חוזה קליטת הניתוח
  דורש שני שחקנים אנושיים, ולכן משחק AI לא נשלח לתור הניתוח הישן.
- התוצאה משויכת לחשבון במאגר שרת המשחק וב־API ההיסטוריה שלו. היסטוריית הטורנירים
  באתר אינה הופכת אוטומטית להיסטוריית משחקי תרגול.
- משחק פתוח יחודש באותה רמה. כדי להחליף רמה יש לסיים או לעזוב אותו.
- הבדיקות האוטומטיות אינן מדידת עומס ייצור או הבטחה לזמן תגובה בשרת אחר.

## בדיקות

- שירות הניתוח: `python manage.py test analysis.tests.test_bot analysis.tests.test_game_state_converter`.
- שרת המשחק: `python manage.py test game.tests.test_ai game.tests.test_ai_socket`.
- מול המנוע האמיתי: `python manage.py test game.tests.test_ai_live`, כשחבילת bgsage הנעולה
  זמינה באותו interpreter. הבדיקה משחקת שלושה משחקים מלאים ומאמתת כל תור מול חוקי השרת.
- אתר הטורנירים: `python manage.py test gamelink.test_practice`.
- ממשק האתר: `vitest run src/views/__tests__/PracticeView.test.ts` ובדיקת `vue-tsc --build`.
